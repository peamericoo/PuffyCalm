import { ComingSoon } from "@/components/shared/coming-soon";

export default function NotFound() {
  return (
    <ComingSoon
      title="Page not found"
      description="That page doesn’t exist yet — or the link is off. Head home or browse the collection."
    />
  );
}
